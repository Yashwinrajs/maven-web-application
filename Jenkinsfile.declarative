pipeline {

agent { label 'Tomcat' }

	stages {		
		stage('build') {
    				steps {
					sh 'mvn clean install -Dmaven.test.skip=true'
    					}
  				}
		stage('deploy') {
    				steps {
      					echo "deploy"
    					}
  				}
	
		}
	
}
